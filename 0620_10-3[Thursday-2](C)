#include <stdio.h>

int main(void){
    int a, b;
    int result;
    printf("二つの整数を入力せよ。\n");
    printf("整数a："); scanf("%d",&a);
    printf("整数b："); scanf("%d",&b);
    result = (a+b)/2;
    printf("それらの平均は%dです。", result);
}
